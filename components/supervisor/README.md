# DshanControl supervisor

Conservative health supervisor for the Web service. It uses a failure threshold
and restart cooldown to avoid restart storms. Hardware watchdog feeding is
optional and disabled by default.
