# DshanControl Web

Flask management service implementing authentication, security controls,
industrial adapter endpoints and the shared `/api/v1` platform contract.
Runtime configuration is injected through environment variables; board device
names come from the platform manifest.
